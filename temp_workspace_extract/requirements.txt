# MMB AGENT 24/7 — Python Dependencies
# Generated: 2026-06-07
# Install: pip install -r requirements.txt

# ── Core Web Framework ────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Browser Automation ────────────────────────────────────────────────────────
nodriver>=0.38.0          # CDP-based browser control (no webdriver flag)

# ── HTTP Clients ──────────────────────────────────────────────────────────────
aiohttp>=3.9.0            # Async HTTP (5sim OTP, GeoIP, identity)
requests>=2.31.0          # Sync HTTP (proxy check, GeoIP outbound IP)

# ── Environment & Config ──────────────────────────────────────────────────────
python-dotenv>=1.0.0      # .env file loading

# ── AI Brain (Claude) — Optional ─────────────────────────────────────────────
anthropic>=0.25.0         # Claude API for comment generation
                          # Set ANTHROPIC_API_KEY in .env to enable

# ── System Monitoring — Optional ─────────────────────────────────────────────
psutil>=5.9.0             # RAM-aware concurrency in orchestrator.py
                          # pip install psutil (not required, degrades gracefully)

# ── Standard Library (no install needed) ─────────────────────────────────────
# asyncio      — async/await engine
# hashlib      — SHA-256 profile seeding
# json         — data persistence
# pathlib      — file paths
# re           — regex
# uuid         — unique IDs
# time         — timestamps
# random       — per-profile seeded RNG
# threading    — background loop thread
# contextvars  — per-task audit isolation
# dataclasses  — data models
# string       — session ID generation

# ── Python Version Requirement ────────────────────────────────────────────────
# Python >= 3.10 required (union types X | Y, asyncio.get_running_loop())
# Recommended: Python 3.11 or 3.12
